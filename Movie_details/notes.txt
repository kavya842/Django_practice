 1) project name create chayali
     django-admin startproject projectname

 2) app name create chesi installed apps lo rayali app name
     python manage.py startapp appname

 3) pgadmin lo database create chesi aa database name settings.py lo database lo rayali 
    DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.postgresql",
        "NAME": "movie_details",
        "USER":"postgres",
        "PASSWORD":"root",
        "HOST":"localhost",
        "PORT":5432
    }
}
4) models.py lo 
class Movie(models.Model):
    name=models.CharField(max_length=32)
    director=models.CharField(max_length=32)
    description=models.TextField()
    Cast=models.TextField()
    budget=models.FloatField()
    collections=models.FloatField() 
      ----------models ni create chayali --------

5) python manage.py makemigrations

6)python manage.py migrate

7) python manage.py runserver  --- we see the django opening page we go to admin page later we can create the username and password

8)To create a username and password for django admin panel
---------- python manage.py createsuperuser-----------
  username:kavya
  password:1269kS

9) admin.py lo code rayali:
  from django.contrib import admin
  
  from .models import Movie
  # admin.site.register(Movie)
  @admin.register(Movie)
  class MovieAdmin(admin.ModelAdmin):
    list_display=['name','director','description','collections','budget']
    list_display_links=['director']

10) in django admin panel we can create the data , modify the data, delete the data, read the data without writting the queries

